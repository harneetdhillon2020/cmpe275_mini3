syntax = "proto3";

package election;

service ElectionService {
    rpc GetHash (HashRequest) returns (HashResponse);
}

message HashRequest {
    int32 pid = 1;
    int64 timestamp = 2;
}

message HashResponse {
    string hash_value = 1;
}
